#ifndef SKI_TRACKER_DEFAULTS_HPP
#define SKI_TRACKER_DEFAULTS_HPP

#include <string>

namespace config::defaults {

const std::string& get(const std::string&);

} // namespace config::defaults

#endif
